extend type StoreConfig {
    stripe_mode: String
    stripe_live_pk: String
    stripe_test_pk: String
}

extend type PaymentMethodInput {
    stripe_payments: StripePaymentsInput
}

extend type Cart {
    paymentIntent: String
}

input StripePaymentsInput {
    cc_save: Boolean
    cc_stripejs_token: String!
}
